import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: 'rgba(47,133,130,0.79)',
        },
        secondary: {
            main: '#ffffff',
        },
    },
});

export default theme;