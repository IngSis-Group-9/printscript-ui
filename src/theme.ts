import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            // main: 'rgba(47,133,130,0.95)',
            main: 'rgba(39,77,89,0.88)',
        },
        secondary: {
            main: '#ffffff',
        },
    },
});

export default theme;